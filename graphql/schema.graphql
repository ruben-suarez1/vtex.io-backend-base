type Query {
  tracking(orderId: String!): TrackingResponse! @auth(scope: PUBLIC)
  inventory(skuId: String!, channel: String): InventoryResponse! @auth(scope: PUBLIC)
  availability(skuId: String!, sellerId: String): AvailabilityBySellerResponse! @auth(scope: PUBLIC)
}
